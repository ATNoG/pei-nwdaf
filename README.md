# Intelligence in Action: Achieving Trustworthy AI-Driven Networks

## How to run
1. Setup variables
```bash
cp .env.example .env
vim .env # change configs (Optional)
```

2. Start services
For each folder that has a docker compose you must run `docker compose up -d` while specifying the path to the .env you've created.
